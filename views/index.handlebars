<img src="../public/assets/image/elle.jpg" alt="burgers" class="img-thumbnail">

<div class="row">
  <h2>Burgers not devoured yet!</h2>

  <ul>
    {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block devoured=false}} {{/unless}} {{/each}}
  </ul>
</div>

<div class="row">
  <h2>Burgers already devoured!</h2>

  <ul>
    {{#each burgers}} {{#if devoured}} {{> burgers/burger-block devoured=true}} {{/if}} {{/each}}
  </ul>
</div>

<div class="row">
  <h2>Eat Da Burger!</h2>
  <form id="create-burger" class="create-form">

    <div class="form-group">
      <label for="ca">Burger Name:</label>
      <input type="text" id="ca" name="burger_name">
    </div>
</div>
{{!--
<div class="form-group">
  <label for="devoured">Sleepy?</label><br>
  <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
  <input type="radio" name="sleepy" value="0"> Awake
</div> --}}

<button id="submit-burger" type="submit">Submit</button>
</form>